{
  "//": "BACKLOG-3064 — the type-level controls' compiler, for the SafeSql brand.",
  "//2": "Compiled by electron/services/db/core/__tests__/sqlText.typeControls.test.ts, which",
  "//3": "asserts on tsc's EXIT CODE and its DIAGNOSTIC TEXT. Six of the seven fixtures here are",
  "//4": "SUPPOSED to fail to compile, so this tree is excluded from every root tsconfig and from",
  "//5": "eslint — otherwise `type-check`, `type-check:tests` and `build:electron` would all go red",
  "//6": "on controls that are working. That exclusion is a directory glob",
  "//7": "(`electron/types/__typefixtures__/**`) established by BACKLOG-3067, so this",
  "//8": "subdirectory required NO config change: zero tsconfig edits, zero eslint edits.",
  "//9": "",
  "//10": "This config deliberately does NOT `extends` the root tsconfig.json: the root's `exclude`",
  "//11": "names this directory, and inheriting it would exclude these fixtures from their own",
  "//12": "compiler. The load-bearing compilerOptions are therefore duplicated, and the test",
  "//13": "asserts they still MATCH the root's — a fixture compiled under weaker settings than the",
  "//14": "real code proves nothing about the real code.",
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "moduleResolution": "node",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "strictBindCallApply": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "allowSyntheticDefaultImports": true,
    "noEmit": true,
    "types": ["node"]
  },
  "//include": "Deliberately absent. Each fixture gets a thin config that `extends` this one and names exactly ONE file, so every fixture has its OWN tsc exit code. One fixture per CONDUIT VERB is the point: a single fixture carrying four errors would leave three signatures mutation-blind — revert dbRun to `string` and a combined fixture still exits 2 because dbGet failed."
}
