-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- the header -> transaction_submissions FK is ON DELETE NO ACTION
-- targets: c14
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

ALTER TABLE public.submission_checklists DROP CONSTRAINT submission_checklists_submission_id_fkey;
ALTER TABLE public.submission_checklists ADD CONSTRAINT submission_checklists_submission_id_fkey
  FOREIGN KEY (submission_id) REFERENCES public.transaction_submissions(id) ON DELETE NO ACTION;

DO $proof$
BEGIN
  IF NOT ((SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'submission_checklists_submission_id_fkey') NOT LIKE '%CASCADE%') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ((SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'submission_checklists_submission_id_fkey')) AS mutation;
