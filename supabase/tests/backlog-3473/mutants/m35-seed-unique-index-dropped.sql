-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- the (organization_id, seed_key) partial unique index dropped
-- targets: c17
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

DROP INDEX public.checklist_templates_org_seed_key_key;

DO $proof$
BEGIN
  IF NOT (to_regclass('public.checklist_templates_org_seed_key_key') IS NULL) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('seed idempotency index absent') AS mutation;
