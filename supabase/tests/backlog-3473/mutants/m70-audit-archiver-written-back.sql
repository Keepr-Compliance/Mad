-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- BACKLOG-3474 PR 3 (SR B1): archived_by written back from OLD while archived, undoing the FK's ON DELETE SET NULL
-- targets: c40
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

CREATE OR REPLACE FUNCTION public._checklist_templates_audit()
RETURNS trigger
LANGUAGE plpgsql
SECURITY INVOKER
SET search_path = public
AS $$
BEGIN
  NEW.updated_by := auth.uid();
  IF NEW.archived_at IS NULL THEN
    NEW.archived_by := NULL;
  ELSIF OLD.archived_at IS NULL THEN
    NEW.archived_by := auth.uid();
  ELSE
    NEW.archived_by := OLD.archived_by;
  END IF;
  RETURN NEW;
END
$$;

DO $proof$
BEGIN
  IF NOT (pg_get_functiondef('public._checklist_templates_audit()'::regprocedure) LIKE '%NEW.archived_by := OLD.archived_by%') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('archived_by written back from OLD') AS mutation;
