-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- SELECT on checklist_templates granted to anon
-- targets: c01a
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

GRANT SELECT ON public.checklist_templates TO anon;

DO $proof$
BEGIN
  IF NOT (has_table_privilege('anon', 'public.checklist_templates', 'SELECT')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('anon holds SELECT') AS mutation;
