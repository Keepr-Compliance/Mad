-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- INSERT on submission_checklist_links not granted to authenticated
-- targets: c09p
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

REVOKE INSERT ON public.submission_checklist_links FROM authenticated;

DO $proof$
BEGIN
  IF NOT (NOT has_table_privilege('authenticated', 'public.submission_checklist_links', 'INSERT')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('submission_checklist_links INSERT revoked') AS mutation;
