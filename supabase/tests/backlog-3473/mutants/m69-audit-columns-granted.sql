-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- BACKLOG-3474 PR 3: UPDATE and INSERT granted on updated_by and archived_by
-- targets: c39
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

GRANT UPDATE (updated_by, archived_by), INSERT (updated_by, archived_by) ON public.checklist_templates TO authenticated;

DO $proof$
BEGIN
  IF NOT (has_column_privilege('authenticated', 'public.checklist_templates', 'updated_by', 'UPDATE')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('updated_by updatable') AS mutation;
