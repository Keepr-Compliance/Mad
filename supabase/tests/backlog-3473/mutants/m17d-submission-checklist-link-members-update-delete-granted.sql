-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- UPDATE and DELETE on submission_checklist_link_members granted, with submitter UPDATE / DELETE policies
-- targets: c10
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

GRANT UPDATE, DELETE ON public.submission_checklist_link_members TO authenticated;
CREATE POLICY zz_mutant_submission_checklist_link_members_update ON public.submission_checklist_link_members FOR UPDATE TO authenticated
  USING (EXISTS (SELECT 1 FROM public.transaction_submissions ts WHERE ts.id = submission_checklist_link_members.submission_id AND ts.submitted_by = (SELECT auth.uid())));
CREATE POLICY zz_mutant_submission_checklist_link_members_delete ON public.submission_checklist_link_members FOR DELETE TO authenticated
  USING (EXISTS (SELECT 1 FROM public.transaction_submissions ts WHERE ts.id = submission_checklist_link_members.submission_id AND ts.submitted_by = (SELECT auth.uid())));

DO $proof$
BEGIN
  IF NOT (has_table_privilege('authenticated', 'public.submission_checklist_link_members', 'UPDATE')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('submission_checklist_link_members updatable by the submitter') AS mutation;
