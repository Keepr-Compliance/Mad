-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- BACKLOG-3474 PR 3: the trigger fires only when archived_at is in the UPDATE (archive path only)
-- targets: c37 c38
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

CREATE OR REPLACE TRIGGER checklist_templates_audit
  BEFORE UPDATE OF archived_at ON public.checklist_templates
  FOR EACH ROW
  EXECUTE FUNCTION public._checklist_templates_audit();


DO $proof$
BEGIN
  IF NOT ((SELECT pg_get_triggerdef(t.oid) FROM pg_trigger t WHERE t.tgname = 'checklist_templates_audit' AND t.tgrelid = 'public.checklist_templates'::regclass) LIKE '%UPDATE OF archived_at%') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('trigger on archived_at only') AS mutation;
