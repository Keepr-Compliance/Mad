-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- EXECUTE on _seed_org_checklist_templates granted to authenticated
-- targets: c19
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

GRANT EXECUTE ON FUNCTION public._seed_org_checklist_templates(uuid) TO authenticated;

DO $proof$
BEGIN
  IF NOT (has_function_privilege('authenticated', 'public._seed_org_checklist_templates(uuid)', 'EXECUTE')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('seed function executable') AS mutation;
