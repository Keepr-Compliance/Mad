-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- the write-time override trigger is absent
-- targets: c25a
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

DROP TRIGGER reject_feature_override_above_tier ON public.organization_plans;

DO $proof$
BEGIN
  IF NOT (NOT EXISTS (SELECT 1 FROM pg_trigger WHERE tgrelid = 'public.organization_plans'::regclass AND tgname = 'reject_feature_override_above_tier')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('override trigger absent') AS mutation;
