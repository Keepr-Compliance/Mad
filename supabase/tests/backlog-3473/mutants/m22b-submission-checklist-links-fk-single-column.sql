-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- submission_checklist_links's composite FK replaced by a single-column FK on submission_checklist_item_id
-- targets: c13
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

ALTER TABLE public.submission_checklist_links DROP CONSTRAINT submission_checklist_links_item_fkey;
ALTER TABLE public.submission_checklist_links ADD CONSTRAINT submission_checklist_links_item_fkey FOREIGN KEY (submission_checklist_item_id) REFERENCES public.submission_checklist_items(id) ON DELETE CASCADE;

DO $proof$
BEGIN
  IF NOT ((SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'submission_checklist_links_item_fkey') NOT LIKE '%submission_id%') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ((SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'submission_checklist_links_item_fkey')) AS mutation;
