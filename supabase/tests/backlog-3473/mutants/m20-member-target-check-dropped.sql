-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- the members kind / target CHECK dropped
-- targets: c12a
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

ALTER TABLE public.submission_checklist_link_members DROP CONSTRAINT submission_checklist_link_members_target_check;

DO $proof$
BEGIN
  IF NOT (NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'submission_checklist_link_members_target_check')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('target CHECK absent') AS mutation;
