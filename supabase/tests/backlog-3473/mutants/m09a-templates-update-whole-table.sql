-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- UPDATE on the whole of checklist_templates granted (every column)
-- targets: c07
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

GRANT UPDATE ON public.checklist_templates TO authenticated;

DO $proof$
BEGIN
  IF NOT (has_column_privilege('authenticated', 'public.checklist_templates', 'seed_key', 'UPDATE')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('seed_key updatable') AS mutation;
