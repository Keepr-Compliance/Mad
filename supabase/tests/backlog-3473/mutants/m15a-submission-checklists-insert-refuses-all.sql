-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- submission_checklists INSERT policy WITH CHECK (false) (too strict: refuses the submitter)
-- targets: c09p
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

DROP POLICY IF EXISTS submission_checklists_insert ON public.submission_checklists;
CREATE POLICY submission_checklists_insert ON public.submission_checklists FOR INSERT TO authenticated WITH CHECK (false);

DO $proof$
BEGIN
  IF NOT ((SELECT with_check FROM pg_policies WHERE schemaname = 'public' AND policyname = 'submission_checklists_insert') = 'false') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('submission_checklists insert refuses everyone') AS mutation;
