-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- SELECT on checklist_seed_templates granted to authenticated
-- targets: c19
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

GRANT SELECT ON public.checklist_seed_templates TO authenticated;

DO $proof$
BEGIN
  IF NOT (has_table_privilege('authenticated', 'public.checklist_seed_templates', 'SELECT')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('catalogue selectable') AS mutation;
