-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- submission_checklist_items's composite FK replaced by a single-column FK on submission_checklist_id
-- targets: c13
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

ALTER TABLE public.submission_checklist_items DROP CONSTRAINT submission_checklist_items_checklist_fkey;
ALTER TABLE public.submission_checklist_items ADD CONSTRAINT submission_checklist_items_checklist_fkey FOREIGN KEY (submission_checklist_id) REFERENCES public.submission_checklists(id) ON DELETE CASCADE;

DO $proof$
BEGIN
  IF NOT ((SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'submission_checklist_items_checklist_fkey') NOT LIKE '%submission_id%') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ((SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'submission_checklist_items_checklist_fkey')) AS mutation;
