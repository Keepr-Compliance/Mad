-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- INSERT on the whole of checklist_templates granted (every column)
-- targets: c07
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

GRANT INSERT ON public.checklist_templates TO authenticated;

DO $proof$
BEGIN
  IF NOT (has_column_privilege('authenticated', 'public.checklist_templates', 'seed_key', 'INSERT')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('seed_key insertable') AS mutation;
