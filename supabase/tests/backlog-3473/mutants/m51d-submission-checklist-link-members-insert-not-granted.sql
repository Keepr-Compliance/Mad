-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- INSERT on submission_checklist_link_members not granted to authenticated
-- targets: c09p
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

REVOKE INSERT ON public.submission_checklist_link_members FROM authenticated;

DO $proof$
BEGIN
  IF NOT (NOT has_table_privilege('authenticated', 'public.submission_checklist_link_members', 'INSERT')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('submission_checklist_link_members INSERT revoked') AS mutation;
