-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- the catalogue name CHECK dropped
-- targets: c24
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

ALTER TABLE public.checklist_seed_templates DROP CONSTRAINT checklist_seed_templates_name_check;

DO $proof$
BEGIN
  IF NOT (NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'checklist_seed_templates_name_check')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('name CHECK absent') AS mutation;
