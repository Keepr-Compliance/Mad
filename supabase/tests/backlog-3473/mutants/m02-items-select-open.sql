-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- checklist_template_items SELECT policy USING (true)
-- targets: c01
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

DROP POLICY IF EXISTS checklist_template_items_select_member ON public.checklist_template_items;
CREATE POLICY checklist_template_items_select_member ON public.checklist_template_items
  FOR SELECT TO authenticated
  USING (true);


DO $proof$
BEGIN
  IF NOT ((SELECT qual FROM pg_policies WHERE schemaname = 'public' AND policyname = 'checklist_template_items_select_member') = 'true') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('items readable by any authenticated user') AS mutation;
