-- BACKLOG-3473 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- checklist_templates SELECT policy USING (true)
-- targets: c01
-- Runs INSIDE a control's transaction, after the prelude; rolled back with it.

DROP POLICY IF EXISTS checklist_templates_select_member ON public.checklist_templates;
CREATE POLICY checklist_templates_select_member ON public.checklist_templates
  FOR SELECT TO authenticated
  USING (true);


DO $proof$
BEGIN
  IF NOT ((SELECT qual FROM pg_policies WHERE schemaname = 'public' AND policyname = 'checklist_templates_select_member') = 'true') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('templates readable by any authenticated user') AS mutation;
