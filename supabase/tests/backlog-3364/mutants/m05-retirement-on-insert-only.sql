-- BACKLOG-3364 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- the retirement trigger fires on INSERT only (a claim by UPDATE is missed)
-- Runs INSIDE a control's transaction, after lib/fixtures.sql; rolled back with it.

CREATE OR REPLACE TRIGGER retire_personal_membership
  AFTER INSERT ON public.organization_members
  FOR EACH ROW
  WHEN (NEW.user_id IS NOT NULL)
  EXECUTE FUNCTION public._retire_personal_membership();

DO $proof$
BEGIN
  IF NOT ((SELECT pg_get_triggerdef(oid) FROM pg_trigger WHERE tgrelid='public.organization_members'::regclass AND tgname='retire_personal_membership') NOT LIKE '%UPDATE%') THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ((SELECT pg_get_triggerdef(oid) FROM pg_trigger WHERE tgrelid='public.organization_members'::regclass AND tgname='retire_personal_membership')) AS mutation;
