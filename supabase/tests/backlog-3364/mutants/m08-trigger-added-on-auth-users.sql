-- BACKLOG-3364 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- a sign-up trigger on auth.users that calls ensure
-- Runs INSIDE a control's transaction, after lib/fixtures.sql; rolled back with it.

CREATE FUNCTION public._t3364_mutant_signup_ensure() RETURNS trigger
LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $m$
BEGIN
  PERFORM public._ensure_personal_organization_for(NEW.id);
  RETURN NEW;
END
$m$;
CREATE TRIGGER t3364_mutant_signup_ensure AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public._t3364_mutant_signup_ensure();

DO $proof$
BEGIN
  IF NOT (EXISTS (SELECT 1 FROM pg_trigger WHERE tgrelid='auth.users'::regclass AND tgname='t3364_mutant_signup_ensure')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('trigger t3364_mutant_signup_ensure on auth.users') AS mutation;
