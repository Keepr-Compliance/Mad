-- BACKLOG-3364 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- the retirement trigger on organization_members dropped
-- Runs INSIDE a control's transaction, after lib/fixtures.sql; rolled back with it.

DROP TRIGGER retire_personal_membership ON public.organization_members;

DO $proof$
BEGIN
  IF NOT (NOT EXISTS (SELECT 1 FROM pg_trigger WHERE tgrelid='public.organization_members'::regclass AND tgname='retire_personal_membership')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('retirement trigger absent') AS mutation;
