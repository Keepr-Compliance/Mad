-- BACKLOG-3364 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- the column guard trigger on organizations dropped
-- Runs INSIDE a control's transaction, after lib/fixtures.sql; rolled back with it.

DROP TRIGGER guard_personal_owner_user_id ON public.organizations;

DO $proof$
BEGIN
  IF NOT (NOT EXISTS (SELECT 1 FROM pg_trigger WHERE tgrelid='public.organizations'::regclass AND tgname='guard_personal_owner_user_id')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('guard trigger absent') AS mutation;
