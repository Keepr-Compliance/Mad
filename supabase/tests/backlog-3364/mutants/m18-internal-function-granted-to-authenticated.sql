-- BACKLOG-3364 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- EXECUTE on the internal function granted to authenticated
-- Runs INSIDE a control's transaction, after lib/fixtures.sql; rolled back with it.

GRANT EXECUTE ON FUNCTION public._ensure_personal_organization_for(uuid) TO authenticated;

DO $proof$
BEGIN
  IF NOT (has_function_privilege('authenticated', 'public._ensure_personal_organization_for(uuid)', 'EXECUTE')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ('authenticated holds EXECUTE on the internal function') AS mutation;
