-- BACKLOG-3364 MUTANT (generated by generate.mjs from the shipped files; do not hand-edit)
-- storage.objects upload policy restored to production's text (no personal clause)
-- Runs INSIDE a control's transaction, after lib/fixtures.sql; rolled back with it.

DROP POLICY IF EXISTS "Members can upload submission attachments" ON storage.objects;
CREATE POLICY "Members can upload submission attachments" ON storage.objects
  FOR INSERT TO public
  WITH CHECK ((bucket_id = 'submission-attachments'::text) AND (split_part(name, '/'::text, 1) IN ( SELECT (organization_members.organization_id)::text AS organization_id
     FROM public.organization_members
    WHERE (organization_members.user_id = auth.uid()))));

DO $proof$
BEGIN
  IF NOT (NOT EXISTS (SELECT 1 FROM pg_policies WHERE schemaname='storage' AND tablename='objects' AND policyname='Members can upload submission attachments' AND with_check LIKE '%personal_owner_user_id%')) THEN
    RAISE EXCEPTION 'MUTATION NOT APPLIED';
  END IF;
END
$proof$;
SELECT 'MUTATION APPLIED: ' || ((SELECT with_check FROM pg_policies WHERE schemaname='storage' AND tablename='objects' AND policyname='Members can upload submission attachments')) AS mutation;
